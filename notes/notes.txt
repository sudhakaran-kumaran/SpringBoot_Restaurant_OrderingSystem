$(document).ready(function (){
  console.log("hello");
  $("p").css("background-color","yellow");   //for the paragraphn tag it will run 
});


$(#first_form).submit(function (e)
{
    e.preventDefault();
    var first_name = $("#first_name").val();
    var last_name = $("#last_name").val();

    if(first_name.length < 1){
        $("$=#first_name).after(
            "<span class="error"> 
        )
    }
    })

    AJAX function : 

    
